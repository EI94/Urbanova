name: Firestore CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'firestore.rules'
      - 'firestore.indexes.json'
      - 'firebase.json'
      - '.github/workflows/firestore-ci.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'firestore.rules'
      - 'firestore.indexes.json'
      - 'firebase.json'
      - '.github/workflows/firestore-ci.yml'

jobs:
  validate-firestore:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: npm ci
        
      - name: Install Firebase CLI
        run: npm install -g firebase-tools
        
      - name: Validate Firestore rules
        run: firebase firestore:rules:validate
        
      - name: Validate Firestore indexes
        run: firebase firestore:indexes:validate
        
      - name: Check rules and indexes sync
        run: |
          echo "🔍 Verifica sincronizzazione rules/indexes..."
          
          # Verifica che firestore.rules esista
          if [ ! -f "firestore.rules" ]; then
            echo "❌ firestore.rules non trovato"
            exit 1
          fi
          
          # Verifica che firestore.indexes.json esista
          if [ ! -f "firestore.indexes.json" ]; then
            echo "❌ firestore.indexes.json non trovato"
            exit 1
          fi
          
          # Verifica che firebase.json referenzi entrambi i file
          if ! grep -q "firestore.rules" firebase.json; then
            echo "❌ firebase.json non referenzia firestore.rules"
            exit 1
          fi
          
          if ! grep -q "firestore.indexes.json" firebase.json; then
            echo "❌ firebase.json non referenzia firestore.indexes.json"
            exit 1
          fi
          
          echo "✅ Rules e indexes sono sincronizzati"
          
      - name: Run migration dry-run
        run: npm run migrate:dry-run
        env:
          FIREBASE_PROJECT_ID: urbanova-dev
          
      - name: Run migration validation
        run: npm run migrate:validate
        
      - name: Test build
        run: npm run build
        
      - name: Success
        run: |
          echo "🎉 Firestore CI completato con successo!"
          echo "✅ Regole validate"
          echo "✅ Indici validati"
          echo "✅ Sincronizzazione verificata"
          echo "✅ Migrazione dry-run completata"
          echo "✅ Build test completato"
