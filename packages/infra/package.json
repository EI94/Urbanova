{
  "name": "@urbanova/infra",
  "version": "0.1.0",
  "description": "Infrastructure utilities for Urbanova",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.0.0",
    "@urbanova/types": "file:../types",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.0"
  },
  "peerDependencies": {
    "firebase-admin": "^12.0.0"
  }
}
