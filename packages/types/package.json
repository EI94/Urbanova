{
  "name": "@urbanova/types",
  "version": "1.0.0",
  "description": "Shared types and Zod schemas for Urbanova",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "typescript": "^5.8.2"
  },
  "peerDependencies": {
    "firebase": "^11.10.0"
  }
}
